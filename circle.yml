machine:
  java:
    version: openjdk8
dependencies:
  override:
    - ./install-dependencies.sh
test:
  override:
    - ./run.sh
  post:
    - mkdir -p $CIRCLE_TEST_REPORTS/junit/
    - find . -type f -regex ".*/reports/.*xml" -exec cp {} 
