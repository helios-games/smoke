general:
  artifacts:
    - errorShots
test:
  override: 
    - mkdir junit
    - find . -name wdio
    - ./node_modules/webdriverio/bin/wdio wdio.conf.js
  post:
    - mv junit $CIRCLE_TEST_REPORTS/
