version: 2
jobs:
  build:
    working_directory: ~/work
    docker:
      - image: "circleci/openjdk:8"
    steps:
      - checkout
      - run: sudo apt-get -y install npm nodejs
      - restore_cache:
          key: dependency-cache-{{ checksum "package.json" }}
      - run: npm install
      - save_cache:
          key: dependency-cache-{{ checksum "package.json" }}
          paths:
            - node_modules
      - run: mkdir junit
      - run: cat ./node_modules/wdio/node_modules/.bin/wdio 
      - run: ./node_modules/wdio/node_modules/.bin/wdio wdio.conf.js
      - store_test_results:
          path: junit
      - store_artifacts:
          path: errorShots
          destination: errorShots
      - run: "curl -f -u $CIRCLE_TOKEN: -X POST https://circleci.com/api/v1.1/project/github/phoebus-games/load-test/tree/master"
